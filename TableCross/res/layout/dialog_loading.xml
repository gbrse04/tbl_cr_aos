<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="220dp"
    android:layout_height="150dp"
    android:background="@drawable/bg_roudrect_white"
    android:orientation="vertical"
    tools:context=".dialog.DialogLoading" >

    <TextView
        style="@style/Text.Title.Bold"
        android:layout_margin="10dp"
        android:drawableLeft="@drawable/logo_app_small"
        android:drawablePadding="10dp"
        android:gravity="center_vertical"
        android:text="@string/app_name"
        android:textColor="@color/orange"
        android:textSize="20sp" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/orange" />

    <LinearLayout
        style="@style/Layout.FillWidthHeight"
        android:gravity="center_vertical"
        android:paddingLeft="20dp" >

        <ProgressBar
            android:id="@+id/progressBarFood"
            style="@style/ProgressBar"
            android:layout_width="40dp"
            android:layout_height="40dp" />

        <TextView
            android:id="@+id/lblMessage"
            style="@style/Text.Title.Black"
            android:layout_margin="10dp"
            android:gravity="center_vertical"
            android:text="@string/loading" />
    </LinearLayout>

</LinearLayout>